export type Banner = {
    title: string;
    body: string;
    id: string;
    image: string;
    price: string;
    priceWithDiscount: string;
}

export type BannerWrapper = {
    productId: string;
}