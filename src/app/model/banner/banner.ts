export type Banner = {
    name: string;
    description: string;
    id: string;
    image: string;
    price: number;
    priceWithDiscount: number;
};

export type BannerWrapper = {
    productId: string;
};
