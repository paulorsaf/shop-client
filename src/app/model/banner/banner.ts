export type Banner = {
    title: string;
    description: string;
    id: string;
    image: string;
    price: string;
    priceWithDiscount: string;
};

export type BannerWrapper = {
    productId: string;
};
